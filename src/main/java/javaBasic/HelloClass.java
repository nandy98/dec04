package javaBasic;

public class HelloClass {

	 public static void main(String ...args) {
		//...args is used to say variable args to the compiler
		//Dot(.)is called ellipses
		//javac HelloClass.java is use to compile the java program
		//java HelloClass is use to run the command 
		//set the java path to resolve the error "Javac is not recognized as external or internal command"
		// can save source file and class file with different name is class is not public
		//we can have multiple classes in a source file
		//we don't need to set path if our source file is within java bin directory
		 System.out.println("Hello");
	}
	 
}
